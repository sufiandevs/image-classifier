# image-classifier
This project implements an end-to-end image classification pipeline using the CIFAR-10 dataset with classical machine learning algorithms. It demonstrates how traditional ML models perform on image data after preprocessing and dimensionality reduction.
